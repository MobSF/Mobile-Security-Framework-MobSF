# Internationalization (39)

- Default languages: English (`en`), Portuguese (`pt`).
- Strings wrapped with Django's `gettext_lazy`; translation files stored in `locale/`.
- Language selector persists preference in user profile and session cookie.
- Documentation includes Portuguese translations in `docs/pt-BR/`.
